bricks:
  feature_brick:
    path: ./bricks/feature_brick
  feature_core_brick:
    path: ./bricks/feature_core_brick
  app_ui: ^0.0.4

# Note
# Run "mason upgrade" when add new bricks

# Feature Brick
# Create:
# mason make feature_brick --feature_name change_password --folder_name change_password --state_management cubit

# Feature Core Brick
# Create:
# mason make feature_core_brick  --feature_name user_order --state_management cubit